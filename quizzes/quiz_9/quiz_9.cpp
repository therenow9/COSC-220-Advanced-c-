#include <iostream>

using namespace std;

void setValue(int arr[],int n,int value)
{
    for (int i=0; i<n; i++)
    {
        arr[i]=value;
    }
    //all integer arrays returned automatically by reference
}
